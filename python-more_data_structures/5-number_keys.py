#!/usr/bin/python3
def number_keys(a_dictionary):
    """Return the number of keys in a dictionary

    Args:
        a_dictionary: the dictionary to inspect

    Returns:
        The number of keys in a_dictionary
    """
    return len(a_dictionary)
