# python-hello_world

This project contains a simple shell script that runs a Python file
whose name is provided via the `$PYFILE` environment variable.

## Task 0: Run Python file

`0-run` is a Bash script that executes the Python file specified in
the `$PYFILE` environment variable.

### Usage

```
export PYFILE=main.py
./0-run
```
